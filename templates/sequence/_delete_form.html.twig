<form method="post" action="{{ path('app_sequence_delete', {'id': sequence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sequence.id) }}">
    <button class="btn">Delete</button>
</form>
